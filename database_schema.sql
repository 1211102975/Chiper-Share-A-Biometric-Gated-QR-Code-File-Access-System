-- SQL Server Database Schema for Chiper Share System
-- Converted from MySQL syntax to SQL Server syntax

-- Users table (already exists, but here's the reference)
-- Note: Your existing Users table should have: user_id, name, email, password
-- Add profile_picture column if it doesn't exist
